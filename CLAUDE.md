# MMD Web Viewer — ブラウザで動くMMDフィギュアビューアー

> スマホのブラウザだけで動くMMD（MikuMikuDance）モデルビューアー。PMXモデルとVMDモーションをその場で読み込んで再生し、スワイプで回転、さらにスマホの姿勢（ジャイロ）でカメラが動くことで「フィギュアを手に持って眺める」体験を実現する。GitHub Pagesで配信する完全静的サイトであり、開発はGitHub Actions上のClaude Codeで行う。

## 背景・文脈

X（Twitter）で見かけた「Claude+CodexでWebブラウザ上で動くMMDビューアーを作った」という投稿が発端。モデル読み込み・VMD再生に加え、スマホの姿勢ベースでカメラ位置が変わるため、実物のフィギュアを持って眺めているような鑑賞体験になっていた。これと同等のものを自分の開発ワークフロー（GitHub Issue/PRで `@claude` に指示 → Actions上で実装 → Pagesへ自動デプロイ → スマホ実機で確認）で作る。

開発者はローカルに開発環境を常時持てない制約があるため、**リポジトリへのIssue起票だけで開発が進む**ことがこのプロジェクトの重要な性質である。ビルドステップを持たない構成はこの制約への対応でもある。

## 目標

**短期（MVP）**: GitHub Pagesで公開されたページをスマホで開き、手持ちのPMXモデルとVMDモーションを読み込んで、スワイプ回転・ピンチズーム・姿勢カメラで鑑賞できる状態。

**成功の定義**: 「フィギュアを持っている感覚」が成立していること。具体的には、スマホを傾けたときにカメラが遅延なく（体感60fps・気持ち悪さのない写像で）追従し、ダンスモーションが物理演算（スカート・髪の揺れ）込みで再生されること。

## 技術スタック

- **Three.js**（CDNのimportmap経由。npm/バンドラ不使用。**バージョンは0.170.0に固定** — MMDLoader/MMDAnimationHelperはr172でthree/addonsから削除されるため、安易にバージョンを上げないこと）
- **MMDLoader / MMDAnimationHelper**（three/addons。PMX/PMD読み込みとVMD再生を担う）
- **ammo.js**（MMDの剛体物理。MMDAnimationHelperが要求する。CDNから読み込み）
- **OrbitControls**（three/addons。スワイプ回転・ピンチズームに使用）
- **DeviceOrientation API**（姿勢カメラ用）
- ビルドツール・フレームワークは**一切使わない**。`index.html` + ESモジュールのプレーンなJSのみ。

## リポジトリ構成

```
/
├── CLAUDE.md
├── index.html          # エントリポイント（UI含む）
├── src/
│   ├── main.js         # 初期化・レンダリングループ
│   ├── loader.js       # PMX/VMDのファイル読み込み処理
│   ├── camera.js       # OrbitControls + 姿勢カメラの合成
│   └── ui.js           # ファイル選択・トグル等の最小UI
└── .github/workflows/
    ├── deploy.yml      # GitHub Pagesデプロイ
    └── claude.yml      # Claude Code Action（@claude応答）
```

ファイル分割は目安であり、実装の都合で統合してよい。ただしindex.html一枚に全部詰め込むことは避け、責務ごとにモジュールを分けること。

## 機能仕様

### 1. モデル読み込み（PMX）

- `<input type="file" multiple>` またはフォルダ選択（`webkitdirectory`）でユーザーがローカルのモデル一式を選択する。
- **重要な実装注意**: PMXはテクスチャを相対パスで参照するが、MMDLoaderはURLベースで動作する。選択された `File` 群を `URL.createObjectURL()` でBlob URL化し、`LoadingManager.setURLModifier()` で「PMX内の相対パス → 対応するBlob URL」に解決するマッピング層を実装すること。ファイル名の大文字小文字・パス区切り（`\` と `/`）・全角文字の正規化に注意。これがこのプロジェクト最大のハマりポイントである。
- ZIPアップロード対応は任意（対応する場合はfflate等の軽量ライブラリをCDNから）。MVPでは複数ファイル選択で足りる。

### 2. モーション再生（VMD）

- VMDファイルを別途選択し、`MMDAnimationHelper` でモデルにバインドして再生する。
- ammo.jsによる物理演算（スカート・髪・胸の揺れ）を有効にする。物理ON/OFFトグルをUIに置く（低スペック端末向け）。
- 再生/一時停止ボタン。ループ再生をデフォルトとする。
- モーション未読込時はモデルをAスタンド（初期ポーズ）のまま表示する。

### 3. カメラ操作

- **スワイプ回転**: OrbitControlsの水平回転。慣性（damping）を有効にする。
- **ピンチズーム**: OrbitControlsのdolly。近接・遠方のクランプを設定する。
- **姿勢カメラ**: DeviceOrientationのジャイロ値をカメラ位置のオフセットに写像する。回転をそのままカメラに適用するのではなく、**端末の傾きを視点の平行移動＋注視点固定のパララックス**として写像する方が「手に持っている」感覚に近い（要実機調整。写像の設計はUnresolvedに記載）。
- 姿勢カメラはON/OFFトグル必須。OFF時はOrbitControlsのみ。ON時もスワイプ回転と共存させる（姿勢はオフセット、スワイプはベース角度として合成）。

### 4. 姿勢カメラの許可フロー

- DeviceOrientationは**HTTPSでのみ動作**する（GitHub Pagesなら自動的に満たされる）。
- iOS Safariでは `DeviceOrientationEvent.requestPermission()` を**ユーザージェスチャ内で**呼ぶ必要がある。姿勢カメラのトグルボタン押下をトリガーにすること。
- 非対応環境（PC等）ではトグルを無効化表示する。

### 5. UI

- モバイルファースト。UIは最小限：モデル読込・モーション読込・再生/停止・姿勢カメラトグル・物理トグルのみ。
- 画面の主役はモデル。UIは半透明の小さなボタン群として隅に置く。
- 床にグリッド（`GridHelper`）、ライトは環境光＋ディレクショナルのシンプル構成。背景色は明るいニュートラル。

## 制約・前提条件

- **MMDモデル・モーションをリポジトリに一切含めない**。PMX/VMDはモデルごとに利用規約（改変・再配布禁止等）が厳格であり、公開リポジトリに置くことは規約違反になる。すべてユーザーがその場でファイル選択する方式とする。サンプルモデルの同梱もしない。
- 完全静的サイト。サーバーサイド処理・ビルドステップなし。
- 開発はGitHub Actions上のClaude Codeで完結させる。ローカル環境を前提とした手順（npm install等）をREADMEに書かない。
- 動作確認の一次ターゲットはAndroidのChrome。iOS Safariは許可フロー含め対応するが、実機確認は後追いでよい。

## 開発ワークフロー

1. IssueまたはPRコメントで `@claude` に実装指示
2. Claude CodeがActions上で実装し、PRを作成
3. mainマージで `deploy.yml` が発火し、GitHub Pagesへ自動デプロイ
4. スマホでPages URLを開いて実機確認、フィードバックを次のIssueへ

コミットは機能単位で小さく。PRには「スマホでの確認手順」を1〜2行で書くこと。

## 受け入れ基準（MVP完了の定義）

1. Pages URLをスマホで開き、PMXモデル（テクスチャ込み）が正しく表示される
2. VMDを読み込むとモーションが物理込みで再生される
3. スワイプで滑らかに回転し、ピンチでズームできる
4. 姿勢カメラONでスマホを傾けると視点が追従し、「持っている」感覚がある
5. モデル・モーションのファイルがリポジトリに含まれていない

## 未解決の問い・不確実性

- **姿勢→カメラの写像設計**: 傾きをそのまま回転に使うか、パララックス的な平行移動にするか、感度・スムージング（ローパスフィルタの時定数）をどうするか。実機での体感調整が必須であり、パラメータはコード先頭に定数としてまとめて調整しやすくしておくこと。
- **ammo.js物理の負荷**: ボーン・剛体数の多いモデルでミッドレンジ端末の60fpsを維持できるか。厳しい場合は物理ステップの間引きを検討。
- **PMXのSDEFスキニング**: MMDLoaderのSDEF対応は不完全であり、一部モデルで変形が崩れる可能性がある。MVPでは既知の制限として許容する。
- **画面回転・スリープ**: 鑑賞中の画面自動回転ロックやWake Lockの要否はMVP後に判断。
